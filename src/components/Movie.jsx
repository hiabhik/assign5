import React, { Component } from 'react'

export default function MovieTitle(MovieName) {
    class Movie extends Component{
        render(){
            return <MovieName name="Your Name"/> 
        }
    }
    return Movie;
}